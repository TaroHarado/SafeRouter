name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
            native_smoke: true
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
            native_smoke: true
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
            native_smoke: false
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
            native_smoke: false
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Smoke test (Windows)
        if: matrix.native_smoke && matrix.os == 'windows-latest'
        run: |
          ./scripts/smoke-release.ps1 -BinaryPath "./target/${{ matrix.target }}/release/cape.exe"

      - name: Smoke test (POSIX)
        if: matrix.native_smoke && matrix.os != 'windows-latest'
        run: |
          chmod +x ./scripts/smoke-release.sh
          ./scripts/smoke-release.sh "./target/${{ matrix.target }}/release/cape"

      - name: Package (Windows)
        if: matrix.archive == 'zip'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../cape-${{ matrix.target }}.zip cape.exe

      - name: Package (Unix)
        if: matrix.archive == 'tar.gz'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../cape-${{ matrix.target }}.tar.gz cape

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            cape-${{ matrix.target }}.${{ matrix.archive }}
          generate_release_notes: true
